name: K8s-file-CD

on:
  push:
    paths:
      - 'k8s/**'

jobs:
  INFRA-ONECLICK-K8S:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      # - name: Ensure pip and yamllint are installed
      #   run: |
      #     if ! command -v pip3 &> /dev/null; then
      #       echo "pip3 not found, installing..."
      #       sudo apt-get update
      #       sudo apt-get install -y python3-pip
      #     fi
      #     pip3 install --upgrade pip
      #     pip3 install yamllint

      # - name: Validate Kubernetes specifications (YAML syntax)
      #   run: |
      #     yamllint k8s-specifications/ || true

      # - name: Dry-run kubectl apply
      #   run: |
      #     kubectl apply --dry-run=client -f k8s-specifications/ -R

      - name: Deploy Kubernetes files
        run: |
          kubectl apply -f k8s/ -R
